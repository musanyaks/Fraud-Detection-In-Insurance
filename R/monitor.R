#' Population Stability Index (PSI)
#' @export
compute_psi <- function(expected, actual, bins = 10) {
  # Simple PSI calculation placeholder
  # In a real app, you'd bin the data and compare distributions
  0.05 # placeholder low drift
}
